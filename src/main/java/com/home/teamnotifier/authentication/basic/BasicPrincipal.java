package com.home.teamnotifier.authentication.basic;

import com.home.teamnotifier.authentication.AnyPrincipal;

public class BasicPrincipal extends AnyPrincipal {
    BasicPrincipal(String name, int id) {
        super(name, id);
    }
}
